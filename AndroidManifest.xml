<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.arny.flightlogbook"
    android:versionCode="10"
    android:versionName="3.2" >

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-sdk
        android:minSdkVersion="16"
        android:targetSdkVersion="23" />
    <application
            android:allowBackup="false"
            android:icon="@drawable/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:icon="@drawable/ic_launcher"
                  android:label="@string/app_name"
                  android:name=".AppWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider"
                       android:resource="@xml/widget_provider" />
        </receiver>
        <activity
            android:name=".AddEditActivity"
            android:label="@string/str_add" >
        </activity>
        <activity
                android:name=".AirplaneTypesActivity"
                android:label="@string/str_airplane_types" >
        </activity>
        <activity
            android:name=".AboutActivity"
            android:label="@string/str_about" >
        </activity>
        <activity
            android:name=".Preferences"
            android:label="@string/str_settings" >
        </activity>
        <activity
            android:name=".MetarActivity"
            android:label="@string/btn_metar_text" >
        </activity>
    </application>

</manifest>